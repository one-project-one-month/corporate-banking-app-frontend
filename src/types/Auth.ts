export type BankAdminLoginPayload = {
  username: string;
  password: string;
};

// lable AccountType in account pages select labe indivaul and organization
// lable don't have organizaiton page (do't have select box in this page)
// lable role in users page select lable have org admin , maker and checker
// label transactionType in deposit transaction page have selcet label annual SquareLibrary,payroll and merchant payment
